import { generateContent, modifyContent } from "./index.js"

// await generateContent(`Make a todo application.`);

await modifyContent(`This is not looking great nor it is functioning well, rewrite this code.`);